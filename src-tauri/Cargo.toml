[package]
name = "openrouter-imagegen-ui"
version = "0.1.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["json", "socks"] }
base64 = "0.22"
rfd = "0.15"

[profile.release]
lto = true
codegen-units = 1
panic = "abort"
